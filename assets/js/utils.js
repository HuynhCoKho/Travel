export const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));export const money=(v,c='VND')=>new Intl.NumberFormat('vi-VN',{style:'currency',currency:c}).format(Number(v)||0);export const date=v=>v?new Intl.DateTimeFormat('vi-VN').format(new Date(v)):'';export const fields=f=>f.map(x=>`<label>${x.label}<${x.type==='textarea'?'textarea':'input'} name="${x.name}" ${x.type&&x.type!=='textarea'?`type="${x.type}"`:''} ${x.required?'required':''} value="${x.type==='textarea'?'':esc(x.value)}">${x.type==='textarea'?esc(x.value):''}${x.type==='textarea'?'</textarea>':''}</label>`).join('');export const csv=rows=>rows.map(r=>r.map(v=>`"${String(v??'').replace(/"/g,'""')}"`).join(',')).join('\n');
