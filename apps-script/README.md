# Google Apps Script backend

Tạo project Apps Script gắn với Spreadsheet, sao chép toàn bộ file `.gs`, chạy `setupSpreadsheet()`, rồi chạy `setAccessToken('một-token-dài-ngẫu-nhiên')`. Triển khai dưới dạng Web app, Execute as **Me**. Không commit token vào repository.
